apiVersion: v1
kind: Service
metadata:
  name: gkx-backend-api
  namespace: gekaixing
spec:
  selector:
    app: gkx-backend-api
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
