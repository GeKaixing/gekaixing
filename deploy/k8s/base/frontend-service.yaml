apiVersion: v1
kind: Service
metadata:
  name: gkx-frontend
  namespace: gekaixing
spec:
  selector:
    app: gkx-frontend
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP
